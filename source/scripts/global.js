const handlerPoint = undefined;
const handlerIcon = undefined;
const handlerPolyline = undefined;
const handlerPolygon = undefined;
const idxTimer = undefined;
const dev = '../static/';
const prod = 'static/';
export default
{
    handlerPoint, // 标绘点
    handlerIcon, // 标绘图标
    handlerPolyline, // 多段线
    handlerPolygon, // 面
    idxTimer, // 定时器
    dev, // 开发环境
    prod, // 生成环境
}
